# 博客使用说明书

> 博客地址：https://1zzhiyuan.github.io/

---

## 一、写新文章

### 第 1 步：新建文章

```bash
npx hexo new "文章标题"
```

在 `source/_posts/` 下会生成一个 `.md` 文件。

### 第 2 步：写内容

用 VSCode 打开那个 `.md` 文件，按这个格式写：

```markdown
---
title: 文章标题
tags: [标签1, 标签2]
categories: [分类]
---

正文内容，用 Markdown 语法写。

## 二级标题

- 列表项
- 列表项

**加粗** *斜体* `代码`

[链接](https://example.com)
```

### 第 3 步：生成静态文件

```bash
npx hexo clean
npx hexo generate
```

### 第 4 步：复制到根目录

把 `public` 文件夹里的**所有文件**，复制到博客根目录（覆盖替换）。

### 第 5 步：上传到 GitHub

```bash
git add .
git commit -m "更新博客"
git push origin main
```

等 1-2 分钟，访问博客就能看到更新了。

---

## 二、修改博客外观

改完下面的文件后，重新走一遍「一、写新文章」的步骤才能生效。

| 想改什么 | 改哪个文件 | 改哪一项 |
|----------|-----------|---------|
| 博客标题 | `_config.yml` | `title:` 后面 |
| 首页副标题 | `_config.fluid.yml` | `slogan.text` |
| 暗色模式 | `_config.fluid.yml` | `dark_mode`（`auto`=自动 / `light`=亮 / `dark`=暗） |
| 导航栏名称 | `_config.fluid.yml` | `blog_title` |
| 关于页的头像和介绍 | `_config.fluid.yml` | `about` 部分 |
| 首页大图 | 替换 `source/img/fluid.png` | 直接换图片文件 |

---

## 三、常用命令速查

| 操作 | 命令 |
|------|------|
| 本地预览 | `npx hexo server` → 浏览器打开 `http://localhost:4000` |
| 清理缓存 | `npx hexo clean` |
| 重新生成 | `npx hexo generate` |
| 查看改了啥 | `git status` |
| 上传更新 | `git add .` → `git commit -m "说明"` → `git push origin main` |

---

## 四、一句话总结

**写文章 → hexo new → 编辑 .md → hexo generate → 复制 public → git push → 等2分钟 → 搞定。**
