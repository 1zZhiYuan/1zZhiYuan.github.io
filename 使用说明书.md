# 使用说明书

## 一、环境准备

```bash
# 安装依赖（项目首次使用）
npm install

# 启动本地预览（浏览器访问 http://localhost:4000）
npx hexo server
# 或 hexo s
# 指定端口：hexo s -p 4001

# 生成静态文件
npx hexo generate
# 或 hexo g

# 清理缓存（生成异常时先运行这个）
npx hexo clean
# 或 hexo cl

# 生成并清理（组合命令）
npx hexo clean && npx hexo generate
```

---

## 二、文章管理

### 新建文章

```bash
npx hexo new "文章标题"
# 会在 source/_posts/ 下创建 "文章标题.md"
```

### 新建草稿

```bash
npx hexo new draft "草稿标题"
# 会在 source/_drafts/ 下创建
# 预览草稿：hexo server --draft
# 发布草稿：hexo publish "草稿标题"
```

### 新建页面

```bash
npx hexo new page "页面名"
# 会在 source/页面名/ 下创建 index.md
# 已有页面：首页、归档、标签、分类、关于、友链、留言板
```

### 文章头部信息（front-matter）

每篇文章开头的 `---` 之间是元数据：

```yaml
---
title: 文章标题
date: 2026-05-01 22:00:00
updated: 2026-05-02 10:00:00   # 可选，更新日期
tags: [标签1, 标签2]            # 标签，可多个
categories: [分类名]            # 分类（只能有一个）
cover: /img/covers/welcome.jpg  # 文章封面图（可选，本地路径或URL）
top_img: /img/default.jpg       # 文章顶部图（可选）
description: 文章摘要           # 可选，不填自动截取
sticky: 1                       # 置顶，数字越大越靠前
comments: true                  # 是否开启评论
copyright: true                 # 是否显示版权声明
---
```

### 文章内标签外挂

````markdown
# 笔记框
{% note primary flat %}
文字内容
{% endnote %}
# 支持样式：primary / success / warning / danger / info

# 标签
{% label primary @标签文字 %}
# 支持样式：primary / success / warning / danger / info / default

# 按钮
{% btn 按钮文字, 链接, 图标名, 颜色 %}
# 示例：{% btn 我的GitHub, https://github.com/1zZhiYuan, fab fa-github, #24292e %}

# 折叠块
{% hideToggle 标题 %}
内容
{% endhideToggle %}

# 选项卡
{% tabs 名称 %}
<!-- tab 标签一 -->
内容
<!-- endtab -->
<!-- tab 标签二 -->
内容
<!-- endtab -->
{% endtabs %}

# 时间线
{% timeline 标题 %}
{% timenode 时间点 %}
内容
{% endtimenode %}
{% endtimeline %}

# Mermaid 图表
{% mermaid %}
graph TD;
  A-->B;
  B-->C;
{% endmermaid %}

# 友链（仅在 link 页面使用）
{% flink %}
- class_name: 分组名
  link_list:
    - name: 站点名
      link: https://...
      avatar: https://.../头像.png
      descr: 站点描述
{% endflink %}
````

---

## 三、部署到 GitHub

### 首次部署

```bash
# 查看远程仓库地址
git remote -v

# 添加所有文件
git add -A

# 提交
git commit -m "提交说明"

# 推送到 GitHub（首次或历史不一致时需要 --force）
git push origin main --force
```

### 更新内容（日常）

```bash
git add -A
git commit -m "更新内容说明"
git push origin main
```

推送后 GitHub Actions 自动构建部署，等待 1-2 分钟访问 https://1zzhiyuan.github.io/

### 查看构建状态

1. 打开 https://github.com/1zZhiYuan/1zZhiYuan.github.io/actions
2. 点击最新一条工作流
3. 绿色勾 ✅ 表示部署成功

---

## 四、配置文件说明

### 主配置（_config.yml）

```yaml
# 站点信息
title: ZhiYuan 的博客         # 博客标题
subtitle: 保持热爱，持续进步    # 副标题
description: 一名大学生程序员... # 站点描述
author: ZhiYuan                # 作者名
language: zh-CN                # 语言
timezone: 'Asia/Shanghai'      # 时区

# 文章链接格式 :year/:month/:day/:title/
permalink: :year/:month/:day/:title/

# 每页文章数
per_page: 8
```

### 主题配置（_config.butterfly.yml）

这是最重要的配置文件，所有外观功能都在这里改：

#### 导航栏菜单

```yaml
menu:
  首页: / || fas fa-home
  归档: /archives/ || fas fa-archive
  标签: /tags/ || fas fa-tags
  # 格式：显示名: 路径 || 图标名
  # 图标名从 https://fontawesome.com 查找
```

#### 社交图标

```yaml
social:
  fab fa-github: https://github.com/1zZhiYuan || GitHub || '#24292e'
  # 格式：图标名: 链接 || 显示名 || 颜色
```

#### 主题色

完整的主题色配置（在 `_config.butterfly.yml` 中 `theme_color` 下）：

```yaml
theme_color:
  enable: true
  main: "#49B1F5"                  # 主色调 - 导航栏/按钮/主要元素
  paginator: "#00c4b6"             # 分页器色 - 页码按钮
  button_hover: "#FF7242"          # 按钮悬停色
  text_selection: "#00c4b6"        # 文字选中背景色
  link_color: "#99a9bf"            # 链接颜色
  meta_color: "#858585"            # 元数据颜色（日期/标签等小字）
  hr_color: "#A4D8FA"              # 分隔线颜色
  code_foreground: "#F47466"       # 代码文字色
  code_background: "rgba(27,31,35,.05)"  # 代码背景色
  toc_color: "#00c4b6"             # 目录高亮色
  blockquote_padding_color: "#49b1f5"   # 引用块左侧竖条色
  blockquote_background_color: "#49b1f5" # 引用块背景色
  scrollbar_color: "#49b1f5"       # 滚动条颜色
  meta_theme_color_light: "ffffff"  # 浏览器主题色（亮色）
  meta_theme_color_dark: "#0d0d0d" # 浏览器主题色（暗色）
```

#### 预设配色方案

将上方对应值替换即可快速换主题色：

| 方案 | 主色 | 分页色 | 悬停色 |
|------|------|--------|--------|
| 简约蓝（默认） | #49B1F5 | #00c4b6 | #FF7242 |
| 活力橙 | #FF6B35 | #F7C59F | #004E89 |
| 经典红 | #E74C3C | #C0392B | #2C3E50 |
| 森林绿 | #27AE60 | #2ECC71 | #1A5276 |
| 暗夜紫 | #8E44AD | #9B59B6 | #2C3E50 |
| 樱花粉 | #FF9A9E | #FECFEF | #A8E6CF |
| 海洋蓝 | #006994 | #00B4D8 | #FF6B6B |

#### CSS 自定义颜色

编辑 `source/css/custom.css` 顶部的 `:root` 变量可修改更多颜色：

```css
:root {
  --card-bg: #fff;              /* 卡片背景色 */
  --body-bg: #f5f5f5;           /* 页面背景色 */
  --footer-bg: #49b1f5;         /* 页脚背景色 */
  --tag-bg: #49b1f5;            /* 标签背景色 */
  --tag-color: #fff;            /* 标签文字色 */
}
```

暗色模式颜色在 `[data-theme="dark"]` 中修改。

---

## 五、图标系统

本博客使用 **Font Awesome 6** 图标。查找图标步骤：

1. 打开 https://fontawesome.com/search
2. 搜索关键词（如 "home", "tag", "heart"）
3. 点击图标复制名称（如 `fa-house`、`fa-tag`、`fa-heart`）
4. 在配置中填入：`fas fa-图标名` 或 `fab fa-图标名`

常见图标：

```
首页    fas fa-home        归档    fas fa-archive
标签    fas fa-tags        分类    fas fa-folder-open
关于    fas fa-heart       友链    fas fa-link
搜索    fas fa-search      日曆    fas fa-calendar
GitHub  fab fa-github     邮箱    fas fa-envelope
B站     fab fa-bilibili    Twitter fab fa-twitter
微信    fab fa-weixin      心形    fas fa-heart
星星    fas fa-star        太阳    fas fa-sun
月亮    fas fa-moon        箭头    fas fa-arrow-right
```

---

## 六、常见修改

### 替换头像
1. 准备一张正方形照片（建议 200x200 像素）
2. 放到 `source/img/` 目录，命名为 `touxiang.jpg`
3. 重新生成：`npx hexo generate`
4. 头像会自动转为 PNG 并显示在侧边栏（不会旋转）

### 替换默认背景图
- 各页面横幅图位于 `source/img/`：
  - `default.jpg` — 首页横幅
  - `archive.jpg` — 归档页
  - `tag.jpg` — 标签页
  - `category.jpg` — 分类页
  - `error.jpg` — 404/错误页
- 覆盖对应文件后重新生成即可

### 修改网站图标（favicon）
- 替换 `source/img/favicon.png`（或改 `_config.butterfly.yml` 的 `favicon` 路径）

### 文章封面图自动生成

每篇文章都有根据标签自动生成的封面图（位于 `source/img/covers/`），各文章封面已自动配置，无需手动设置。

如需手动更换某篇文章封面，编辑文章 front-matter 中的 `cover` 字段：
```yaml
cover: /img/covers/文章名.jpg   # 使用本地图片
# 或
cover: https://example.com/image.jpg  # 使用网络图片
```

### 修改打赏二维码
- 微信：替换 `source/img/wechat-qr.png`
- 支付宝：替换 `source/img/alipay-qr.png`

### 添加新页面

1. 创建页面：`npx hexo new page 页面名`
2. 编辑 `source/页面名/index.md`，在 front-matter 设置：
   ```yaml
   title: 页面标题
   type: 页面名
   comments: true   # 是否开启评论
   ```
3. 在 `_config.butterfly.yml` 的 `menu` 中添加菜单项

### 修改友链
- 编辑 `source/link/index.md`
- 在 `{% flink %}` 标签内增删链接

### 修改关于页面
- 编辑 `source/about/index.md`

### 添加自定义 CSS
- 编辑 `source/css/custom.css`

### 添加自定义 JS
- 编辑 `source/js/custom.js`

### 开启/关闭特效

在 `_config.butterfly.yml` 中修改：

```yaml
canvas_nest:      # 粒子背景
fireworks:        # 点击烟花
click_heart:      # 点击爱心
clickShowText:    # 点击显示文字
preloader:        # 加载动画
```

将 `enable: true` 改为 `false` 即可关闭，反之开启。

### 切换暗色模式

```yaml
darkmode:
  enable: true
  autoChangeMode: 1   # 1=跟随系统, 2=始终暗色, 3=始终亮色
```

### 修改加载动画

```yaml
preloader:
  enable: true
  source: 1   # 1=彩带, 2=齿轮, 3=等
```

---

## 七、完整命令速查表

| 命令 | 说明 |
|------|------|
| `npx hexo s` | 启动本地预览 |
| `npx hexo s -p 4001` | 指定端口启动 |
| `npx hexo g` | 生成静态文件 |
| `npx hexo cl` | 清理缓存 |
| `npx hexo cl && hexo g` | 清理并重新生成 |
| `npx hexo new "标题"` | 新建文章 |
| `npx hexo new draft "标题"` | 新建草稿 |
| `npx hexo publish "标题"` | 发布草稿 |
| `npx hexo new page "页面名"` | 新建页面 |
| `npx hexo list post` | 列出所有文章 |
| `npx hexo list page` | 列出所有页面 |
| `npx hexo list route` | 列出所有路由 |
| `git status` | 查看文件变动 |
| `git add -A` | 暂存所有文件 |
| `git commit -m "说明"` | 提交 |
| `git push origin main` | 推送到 GitHub |
| `git push origin main --force` | 强制推送 |
| `git remote -v` | 查看远程地址 |
| `git log --oneline` | 查看提交历史 |
| `npm install` | 安装依赖 |
| `npm update` | 更新依赖 |

---

## 八、项目目录结构

```
1zZhiYuan.github.io/
├── _config.yml              # Hexo 主配置
├── _config.butterfly.yml    # Butterfly 主题配置
├── package.json             # 依赖管理
├── .gitignore               # Git 忽略规则
├── .github/workflows/       # GitHub Actions 部署配置
├── scaffolds/               # 文章模板
│   ├── post.md
│   ├── page.md
│   └── draft.md
├── source/                  # 源文件目录（主要工作区）
│   ├── _posts/              # 文章（.md 文件）
│   ├── about/               # 关于页面
│   ├── categories/          # 分类页面
│   ├── tags/                # 标签页面
│   ├── link/                # 友链页面
│   ├── messages/            # 留言板页面
│   ├── img/                 # 图片资源
│   │   ├── covers/          # 文章封面图（自动生成）
│   ├── css/                 # 自定义样式
│   └── js/                  # 自定义脚本
├── public/                  # 生成的静态文件（自动生成，不要手动改）
└── node_modules/            # 依赖（自动生成）
```
