# 使用说明书

## 一、环境准备

```bash
# 1. 安装依赖（首次使用）
npm install

# 2. 启动本地预览
npx hexo server
# 浏览器访问 http://localhost:4000
# 按 Ctrl+C 停止预览

# 3. 生成静态文件
npx hexo generate

# 4. 清理缓存（生成异常时先运行这个）
npx hexo clean

# 5. 清理并重新生成（常用组合）
npx hexo clean && npx hexo generate
```

---

## 二、文章管理

### 新建文章

```bash
npx hexo new "文章标题"
# 在 source/_posts/ 下生成 "文章标题.md"
```

### 新建草稿

```bash
npx hexo new draft "草稿标题"
# 预览草稿：hexo server --draft
# 发布草稿：hexo publish "草稿标题"
```

### 新建页面

```bash
npx hexo new page "页面名"
# 在 source/页面名/ 下生成 index.md
```

### 文章头部信息（front-matter）

每篇文章开头 `---` 之间是元数据：

```yaml
---
title: 文章标题
date: 2026-05-01 22:00:00
updated: 2026-05-02 10:00:00    # 可选
tags: [标签1, 标签2]             # 多个标签用逗号分隔
categories: [分类名]             # 分类（只能有一个）
cover: /img/covers/welcome.jpg   # 封面图
top_img: /img/default.jpg        # 顶部横幅图
description: 文章摘要            # 可选
sticky: 1                        # 置顶，数字越大越靠前
comments: true                   # 是否开启评论
---
```

### 文章内特殊语法

````markdown
# 笔记框
{% note primary flat %} 内容 {% endnote %}
# 样式：primary / success / warning / danger / info

# 标签
{% label primary @标签文字 %}
# 样式：primary / success / warning / danger / info / default

# 按钮
{% btn 文字, 链接, 图标, 颜色 %}
# 示例：{% btn GitHub, https://github.com, fab fa-github, #24292e %}

# 折叠块
{% hideToggle 标题 %} 内容 {% endhideToggle %}

# 选项卡
{% tabs 名称 %}
<!-- tab 标签一 --> 内容 <!-- endtab -->
<!-- tab 标签二 --> 内容 <!-- endtab -->
{% endtabs %}

# Mermaid 图表
{% mermaid %}
graph TD; A-->B; B-->C;
{% endmermaid %}

# 友链（仅 link 页面使用）
{% flink %}
- class_name: 分组名
  link_list:
    - name: 站点名
      link: https://...
      avatar: https://.../头像.png
      descr: 站点描述
{% endflink %}
````

---

## 三、部署到 GitHub

### 日常更新

```bash
git add -A
git commit -m "更新说明"
git push origin main
```

推送后 GitHub Actions 自动构建部署，等待 1-2 分钟访问 https://1zzhiyuan.github.io/

### 查看构建状态

打开 https://github.com/1zZhiYuan/1zZhiYuan.github.io/actions
绿色勾表示部署成功。

---

## 四、修改外观（主题配置）

所有外观功能在 `_config.butterfly.yml` 中修改。

### 导航栏菜单

```yaml
menu:
  首页: / || fas fa-home
  归档: /archives/ || fas fa-archive
  标签: /tags/ || fas fa-tags
  # 格式：显示名: 路径 || 图标名
```

### 社交图标

```yaml
social:
  fab fa-github: https://github.com/1zZhiYuan || GitHub || '#24292e'
  # 格式：图标名: 链接 || 显示名 || 颜色
```

### 主题色

在 `theme_color` 下修改各颜色值，需用双引号包裹：

```yaml
theme_color:
  main: "#49B1F5"      # 主色调
  paginator: "#00c4b6" # 翻页按钮色
  button_hover: "#FF7242" # 按钮悬停色
```

### 预设配色方案

替换 `theme_color` 下的对应值：

| 方案 | 主色 | 分页色 | 悬停色 |
|------|------|--------|--------|
| 简约蓝（默认） | #49B1F5 | #00c4b6 | #FF7242 |
| 活力橙 | #FF6B35 | #F7C59F | #004E89 |
| 经典红 | #E74C3C | #C0392B | #2C3E50 |
| 森林绿 | #27AE60 | #2ECC71 | #1A5276 |
| 暗夜紫 | #8E44AD | #9B59B6 | #2C3E50 |
| 樱花粉 | #FF9A9E | #FECFEF | #A8E6CF |
| 海洋蓝 | #006994 | #00B4D8 | #FF6B6B |

### 暗色模式

```yaml
darkmode:
  enable: true
  autoChangeMode: 3   # 1=跟随系统  2=始终暗色  3=始终亮色
```

### 侧边栏模块

```yaml
aside:
  card_author:        # 作者卡片
  card_announcement:  # 公告
  card_recent_post:   # 最新文章（limit: 控制数量）
  card_categories:    # 分类（limit: 控制数量）
  card_tags:          # 标签（limit: 控制数量）
  card_archives:      # 归档（limit: 控制数量）
  card_webinfo:       # 网站资讯
```

### 特效开关

```yaml
canvas_nest:   true   # 粒子背景
fireworks:     true   # 点击烟花
click_heart:   true   # 点击爱心
clickShowText: true   # 点击显示文字
preloader:     true   # 加载动画
```

### 评论系统

```yaml
comments:
  use: giscus
giscus:
  repo: 1zZhiYuan/1zZhiYuan.github.io
```

---

## 五、图片系统

### 各页面横幅图

| 文件路径 | 用途 |
|---------|------|
| `source/img/default.jpg` | 首页顶部横幅 |
| `source/img/archive.jpg` | 归档页横幅 |
| `source/img/tag.jpg` | 标签页横幅 |
| `source/img/category.jpg` | 分类页横幅 |
| `source/img/error.jpg` | 404 错误占位图 |

覆盖对应文件后重新生成即可。

### 头像

1. 准备一张正方形照片
2. 放到 `source/img/` 目录，命名为 `touxiang.jpg`
3. 重新生成：`npx hexo generate`

### 文章封面图

封面图位于 `source/img/covers/`，每篇文章自动配置。如需手动更换某篇文章封面，编辑文章 front-matter 中的 `cover` 字段。

### 打赏二维码

- 微信：替换 `source/img/wechat-qr.png`
- 支付宝：替换 `source/img/alipay-qr.png`

### 网站图标（favicon）

替换 `source/img/favicon.png`，或修改 `_config.butterfly.yml` 中 `favicon` 路径。

---

## 六、常用操作

### 添加新页面

1. `npx hexo new page 页面名`
2. 编辑 `source/页面名/index.md`，修改 front-matter
3. 在 `_config.butterfly.yml` 的 `menu` 中添加菜单项

### 修改友链

编辑 `source/link/index.md`，在 `{% flink %}` 标签内增删链接。

### 添加自定义 CSS

编辑 `source/css/custom.css`。

### 添加自定义 JS

编辑 `source/js/custom.js`。

### 图标查找

使用 Font Awesome 6 图标：https://fontawesome.com/search

常见图标：
```
首页  fas fa-home      归档  fas fa-archive
标签  fas fa-tags      分类  fas fa-folder-open
关于  fas fa-heart     友链  fas fa-link
搜索  fas fa-search    GitHub fab fa-github
邮箱  fas fa-envelope  B站   fab fa-bilibili
```

---

## 七、命令速查

| 命令 | 说明 |
|------|------|
| `npm install` | 安装依赖 |
| `npx hexo s` | 启动本地预览 |
| `npx hexo s -p 4001` | 指定端口启动 |
| `npx hexo g` | 生成静态文件 |
| `npx hexo cl` | 清理缓存 |
| `npx hexo cl && hexo g` | 清理并重新生成 |
| `npx hexo new "标题"` | 新建文章 |
| `npx hexo new draft "标题"` | 新建草稿 |
| `npx hexo publish "标题"` | 发布草稿 |
| `npx hexo new page "页面名"` | 新建页面 |
| `git status` | 查看文件变动 |
| `git add -A` | 暂存所有文件 |
| `git commit -m "说明"` | 提交 |
| `git push origin main` | 推送到 GitHub |

---

## 八、目录结构

```
1zZhiYuan.github.io/
├── _config.yml                  # 主配置（站点信息）
├── _config.butterfly.yml        # 主题配置（外观/功能）
├── package.json                 # 依赖管理
├── .github/workflows/deploy.yml # 自动部署配置
├── scaffolds/                   # 文章模板
│   ├── post.md                  # 文章模板
│   ├── page.md                  # 页面模板
│   └── draft.md                 # 草稿模板
├── source/                      # 源文件（主要工作区）
│   ├── _posts/                  # 文章目录
│   ├── about/                   # 关于页面
│   ├── categories/              # 分类页面
│   ├── tags/                    # 标签页面
│   ├── link/                    # 友链页面
│   ├── messages/                # 留言板页面
│   ├── img/                     # 图片资源
│   │   ├── covers/              # 文章封面图
│   │   ├── touxiang.jpg         # 头像
│   │   ├── avatar.png           # 头像（自动生成）
│   │   ├── default.jpg          # 首页横幅
│   │   ├── archive.jpg          # 归档页横幅
│   │   ├── tag.jpg              # 标签页横幅
│   │   ├── category.jpg         # 分类页横幅
│   │   ├── error.jpg            # 错误占位图
│   │   ├── wechat-qr.png        # 微信打赏码
│   │   └── alipay-qr.png        # 支付宝打赏码
│   ├── css/custom.css           # 自定义样式
│   └── js/custom.js             # 自定义脚本
├── public/                      # 生成的文件（自动生成，勿手动改）
└── node_modules/                # 依赖（自动生成）
```
